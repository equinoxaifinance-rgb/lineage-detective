# Judge-facing Lineage Detective application environment — pinned 2026-07-25.
# It speaks MCP to the isolated DataHub sidecar; it does not import the DataHub
# SDK/CLI or run packaging tooling. Keep the bootstrap installer itself on a
# current audited release.
pip==26.1.2
mcp==1.28.1
anthropic==0.117.0
python-dotenv==1.2.2
streamlit==1.59.2
dbt-duckdb==1.10.1
duckdb==1.5.4
setuptools==83.0.0
