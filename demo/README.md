# Legacy media

`lineage_detective_demo.mp4` is an early historical cut and is not the release candidate.

The current judge artifact is produced from a single uninterrupted browser run by:

1. `tools/record_judge_demo.py`
2. `tools/build_judge_video.py`

Release status, duration, QA measurements, and the bound SHA-256 are recorded in
`LIVE-STATUS.md` and `RELEASE-VERIFICATION.md`.
