# Legacy media

`lineage_detective_demo.mp4` is an early historical cut and is not the release candidate.

The final judge artifact will be produced only after the deployed product and
judge path are current, using:

1. `tools/record_judge_demo.py`
2. `tools/build_judge_video.py`

No current final video is claimed. When it exists, its release status, duration,
QA measurements, and SHA-256 will be recorded in `LIVE-STATUS.md` and
`RELEASE-VERIFICATION.md`.
